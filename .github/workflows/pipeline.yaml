name: Pipeline
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Python install
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          
      - name: Poetry install
        run: pipx install poetry